import { RelativeTimePipePipe } from './relative-time-pipe-pipe';

describe('RelativeTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
